const express = require('mongoose')

const router = express.Router();

router.post('/candidates',async (req, res)=>{
    const body = req.body;

    try{
        
    }
    catch(err){

    }
    
});